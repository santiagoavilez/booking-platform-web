services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "5173:5173"
    env_file:
      - .env.dev
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
